import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: 'row',
        alignItems: 'center'
    },
    inputSearch: {
        width: 200,
        height: 35,
        backgroundColor: '#fff',
        borderColor: 'gray',
        borderWidth: 0
    },
    cart: {
        marginLeft: 10,
        marginRight: 10
    },
    chat: {
        marginRight: 10
    }
});

export default styles