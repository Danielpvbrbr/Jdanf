import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    ar1:{
       
    },
    cart: {
        marginLeft: 2,
        marginRight: 10
        
    },
    chat: {
        marginRight: 0
    }
});

export default styles